{
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "build": "eleventy",
    "deploy": "npm run build && gh-pages -d _site"
  }
}